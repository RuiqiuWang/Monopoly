# `step` 命令

## 格式

```text
step <number>
```

`number` 必须是 `int` 范围内的非负整数。`step 0` 是合法行动：玩家位置不变，但会触发当前位置的落地事件并完成一个回合。

移动距离按 70 格地图取模，因此 `step 70`、`step 140` 等命令位置不变，移动路径为空，但同样触发当前位置事件并完成回合。负数、非整数、溢出值和多余参数均被拒绝。

退出游戏仅使用 `q`。
